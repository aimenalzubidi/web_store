import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2563EB); // Modern Blue
  static const Color primaryDark = Color(0xFF1E3A8A);
  static const Color background = Color(0xFFF3F4F6); // Soft gray background
  static const Color sidebarBackground = Color(
    0xFF111827,
  ); // Dark gray/black for sidebar
  static const Color sidebarItem = Color(
    0xFF374151,
  ); // Gray for unselected sidebar
  static const Color sidebarItemSelected = Color(
    0xFF1F2937,
  ); // Darker gray for selected
  static const Color card = Colors.white;
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color success = Color(0xFF10B981);
  static const Color danger = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
}
